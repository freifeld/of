from _handling_versions import *
from _ArrayOps import ArrayOps



npnot = np.logical_not
npand = np.logical_and
npor = np.logical_or

pi = np.pi
multiply = np.dot 
newaxis = np.newaxis   
#from scipy.linalg import norm,inv
